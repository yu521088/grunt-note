# ??:
1. nodejs
2. ??grunt-cli (npm install -g grunt-cli)


# ????:
1. package.json ????npm??????, ??????????????grunt?Grunt??,???devDependencies?????
		????:
			?? npm init?????????package.json
			?? ?????package.json
			?? ?grunt-init????
2. Gruntfile.js ?????????(task)???Grunt??????????,?package.json??????, ????????????????

# ??Grunt?grunt??:
npm install <module> --save-dev ??????<module>, ??????package.json???devDependencies???, ??:
	npm install grunt --save-dev
	npm install grunt-contrib-jshint --save-dev
	
# Gruntfile???
* “wrapper”??
* ???????
* ??grunt?????
* ?????

??:
module.exports = function(grunt) {							//wrapper??, nodejs???????
  // Project configuration.
  grunt.initConfig({										//???????
    pkg: grunt.file.readJSON('package.json'), 
    uglify: {									//uglify??  (task)
      options: {
        banner: '/*! <%= pkg.name %> <%= grunt.template.today("yyyy-mm-dd") %> */\n'
      },
      build: {									//uglify??? (target)
        src: 'src/<%= pkg.name %>.js',
        dest: 'build/<%= pkg.name %>.min.js'
      }
    }
  });

  // ???? "uglify" ??????
  grunt.loadNpmTasks('grunt-contrib-uglify');				//????

  // ???????????
  grunt.registerTask('default', ['uglify']);				//?????

};

# Grunt??
Grunt??????Gruntfile?grunt.initConfig??????, ?????????????,?????????????????????????
	??????????, ????????,?????JavaScript, ?????????????JavaScript?

## options??(??)
????????,options????????????????;??,?????(target)?????options???target???options???task?
	??options???

## ??(????????????)
Grunt??????????????????????????????src-dest(???-????)???????,???????????????
	???????????(multi-task)?????????,?????????????????????????????src?dest??,???
	??????????????????,?: filter, nonull, dot, matchBase, expand ?
	1. ????: 
		src:['src/aa.js', 'src/bb.js'], dest: 'dest/cc.js'
	2. ??????: ??????????????src-dest???????,?????????,????????(??????????????)?
		files: {
			'dest/a.js': ['src/aa.js', 'src/aaa.js'],
			'dest/a1.js': ['src/aa1.js', 'src/aaa1.js'],
		}
	3. ??????: ??????????????src-dest????,???????????????
		files: [
			{src: ['src/bb.js', 'src/bbb.js'], dest: 'dest/b/', nonull: true},
			{src: ['src/bb1.js', 'src/bbb1.js'], dest: 'dest/b1/', filter: 'isFile'},
		]
	4. ?????: 
		+ '*'?????????,??'/'
		+ '?'??????,??'/'
		+ '**'?????????,??'/'
		+ '{}'????, ?????
		+ '!',????
	5. ??????:??????????????,?????????????????????????????????????????????
		expand ???true?????????:
			+ 'cwd'  ????????
			+ 'src'  ??????cwd?????
			+ 'dest' ?????????
			+ 'ext'  ??????????
			
		??:
		grunt.initConfig({
		  uglify: {
			static_mappings: {
			  // Because these src-dest file mappings are manually specified, every
			  // time a new file is added or removed, the Gruntfile has to be updated.
			  files: [
				{src: 'lib/a.js', dest: 'build/a.min.js'},
				{src: 'lib/b.js', dest: 'build/b.min.js'},
				{src: 'lib/subdir/c.js', dest: 'build/subdir/c.min.js'},
				{src: 'lib/subdir/d.js', dest: 'build/subdir/d.min.js'},
			  ],
			},
			dynamic_mappings: {
			  // Grunt will search for "**/*.js" under "lib/" when the "uglify" task
			  // runs and build the appropriate src-dest file mappings then, so you
			  // don't need to update the Gruntfile when files are added or removed.
			  files: [
				{
				  expand: true,     // Enable dynamic expansion. ??????
				  cwd: 'lib/',      // Src matches are relative to this path. ????
				  src: ['**/*.js'], // Actual pattern(s) to match. ??????
				  dest: 'build/',   // Destination path prefix. ??????
				  ext: '.min.js',   // Dest filepaths will have this extension. ????????
				},
			  ],
			},
		  },
		});

## ??
??<% %>???????????????????????????????????
	1. <%= prop.subprop %> ????????????prop.subprop??
	2. <% %> ???????JavaScript??
	
## ??????
Grunt?grunt.file.readJSON?grunt.file.readYAML??????????JSON?YAML??

??:
grunt.initConfig({
  pkg: grunt.file.readJSON('package.json'),				//??grunt.file.readJSON????JSON??
  uglify: {
    options: {
      banner: '/*! <%= pkg.name %> <%= grunt.template.today("yyyy-mm-dd") %> */\n'
    },
    dist: {
      src: 'src/<%= pkg.name %>.js',
      dest: 'dist/<%= pkg.name %>.min.js'
    }
  }
});